# Database Configuration (Production-ready defaults)
DB_HOST=localhost
DB_NAME=u709288172_builditpc_db
DB_USER=u709288172_sims
DB_PASS=REPLACE_ME
DB_PORT=3306
DB_NAME_PREFIX=u709288172

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# Email Configuration (for Gmail)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_ENCRYPTION=tls

# Preferred auth mode: OAuth2 (wizard will fill the refresh token)
MAIL_AUTH=gmail_oauth
GMAIL_USER=
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# From details
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME="SIMS"

# Debug level (set to 0/false in production)
APP_DEBUG=0
APP_TIMEZONE=Asia/Manila
